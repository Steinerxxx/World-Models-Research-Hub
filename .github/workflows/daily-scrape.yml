name: Daily Paper Scrape

on:
  schedule:
    # Run every hour
    - cron: '0 * * * *'
  workflow_dispatch: # Allow manual trigger from GitHub UI

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Scrape Endpoint
        run: |
          if [ -n "${{ secrets.SCRAPE_URL }}" ]; then
            curl -X POST ${{ secrets.SCRAPE_URL }}
          else
            curl -X POST https://world-models-research-hub-backhend.onrender.com/api/scrape
          fi
